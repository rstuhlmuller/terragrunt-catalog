branches:
  - main
tagFormat: "${version}"
plugins:
  - - "@semantic-release/commit-analyzer"
    - releaseRules:
        - type: build
          release: patch
        - type: chore
          release: patch
        - type: ci
          release: patch
        - type: docs
          release: patch
        - type: refactor
          release: patch
        - type: style
          release: patch
        - type: test
          release: patch
  - "@semantic-release/release-notes-generator"
  - "@semantic-release/github"
